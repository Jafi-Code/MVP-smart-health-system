[build]
builder = "RAILPACK"
buildCommand = "npm install --workspaces --include-workspace-root && cd apps/api && npx prisma generate && npm run build"

[deploy]
startCommand = "cd apps/api && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3